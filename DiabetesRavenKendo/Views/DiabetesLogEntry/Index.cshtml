<<<<<<< HEAD
@model IEnumerable<DiabetesRavenKendo.Models.DiabetesLogEntryModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<ul data-role="listview" data-theme="b">
    @foreach (var item in Model) {
    <li>@Html.ActionLink(
                            string.Format("{0} {1} mg/mL"
                                        ,item.Time.ToString("g",System.Globalization.DateTimeFormatInfo.InvariantInfo)
                                        ,item.GlucoseReading.Glucose)
                            , "Details"
                            , new { id = item.Id}
                        )
    </li>
    }
</ul>
@*<table>
    <tr>
        <th></th>
        <th>
            Date
        </th>
        <th>
            Glucose
        </th>
        <th>
            Location
        </th>
        <th>
            Comments
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
        <td>
			@String.Format("{0:g}", item.Date)
        </td>
        <td>
            @String.Format("{0} mg/dL", item.GlucoseReading.Glucose)
        </td>
        <td>
            @String.Format("{0}", item.GlucoseReading.Location)
        </td>
        <td>
			@item.Comments
        </td>
    </tr>
}

</table>

*@
=======
@using DiabetesRavenKendo.Models;
@model IEnumerable<DiabetesRavenKendo.Models.DiabetesLogEntryModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Date
        </th>
        <th>
            Glucose
        </th>
        <th>
            Location
        </th>
        <th>
            Lantus
        </th>
        <th>
            Comments
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
        <td>
			@String.Format("{0:g}", item.Date)
        </td>
        <td>
            @String.Format("{0} mg/dL", item.GlucoseReading.Glucose)
        </td>
        <td>
            @String.Format("{0}", item.GlucoseReading.Location)
        </td>
        <td>
            @String.Format("{0:0.##}", (null == item[InsulinEnum.Lantus]) ? 0 : item[InsulinEnum.Lantus].Units)
        </td>
        <td>
			@item.Comments
        </td>
    </tr>
}

</table>


>>>>>>> b8018593a5238f699c8890bb945d5d1d4b39d1a0
